
#include <stdio.h>
int main() {
  int a[3][3], b[3][3], mul[3][3], i, j;

printf("enter 9 number in matrix a\n");
for(int i =0; i<=2; i++)
   for(j = 0; j<=2; j++)
   scanf("%d ",&a[i][j]);
 printf("enter 9 number in matrix b\n");
 for(int i =0; i<=2; i++)
   for(j = 0; j<=2; j++)
   scanf("%d ",&b[i][j]); 
    printf("multiplication of two matrix is\n");
    for(i = 0; i<=2; i++){
       for(j=0; j<=2; j++){
        mul[i][j] = a[i][j] * b[i][j];
        printf("%d ",mul[i][j]);
       }
       printf("\n");
    }
        
  return 0;

}
