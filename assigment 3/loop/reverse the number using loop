
#include <stdio.h>

int main() {
    int rmdr,sum=0,num,t;
    printf("enter the number to reverse it\n ");
    scanf("%d",&num);
   for(t=num;num!=0;num=num/10){
         rmdr=num % 10;
         sum=sum*10+rmdr;
 } printf("the reverse number is %d",sum);
  
    return 0;
}
