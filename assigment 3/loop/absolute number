#include<stdio.h>
int main(){
    int n,num;
    printf("enter the number\n");
    scanf("%d",&n);
    num=n;
    if(num<0)
   { num= -1*num;}
    printf("the absolute number of %d = %d",n,num );
    
}
