#include <stdio.h>
int main(){
    int year;
    printf("enter the year to find weather its leap year or not\n" );
    scanf("%d",&year);
    if (year / 4 == 0)
       printf("%d is an leap year",year);
    else
       printf("%d is not a leap year",year);
}
